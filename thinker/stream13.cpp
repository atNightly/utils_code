#include <iostream>
#include <

/*
 *  格式化类标志
 *  ios::dec    使用整型数值的基数为10(十进制形式)
 *  ios::hex    使整型数值的基数为16
 *  ios::oct    使整型数值的基数为8
 *  ios::fixed  按照固定格式显示浮点数
 *  ios::left   数值左对齐,使用填充字符填充右边的空位
 *  ios::right  数值右对齐,使用填充字符填充左边空位
 *  ios::internal 把填充字符放到前导正负号或基数指示符之后
 *
 *  宽度 填充和精度设置
 *  ios::width 返回当前宽度
 *  ios::width(int n) 设置宽度
 *  ios::fill() 返回当前填充字符
 *  ios::fill(int n)    设定填充字符
 *  ios::precision()    返回当前浮点数精度
 *  ios::precision(int n) 设定浮点数精度
 */
